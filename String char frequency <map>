//String character frequency//
#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin>>s;
    map<char,int>m;
    for(int i=0; i<s.size(); i++)
    {
        m[s[i]]++;
    }
    for(auto it:m)
    {
        cout<<it.first<<" "<<it.second<<endl;
    }
    return 0;
}
